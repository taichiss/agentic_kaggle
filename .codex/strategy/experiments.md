# 実験ログ

## テンプレート
| 日付 | テーマ | 変更点 | 検証方法 | 結果 | 学び | 次 |
| --- | --- | --- | --- | --- | --- | --- |

## ログ

## テンプレート
| 日付 | テーマ | 変更点 | 検証方法 | 結果 | 学び | 次 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-01 | discussion 監査 | `doc/discussion/2026` を精読し、主要主張をローカル CSV で照合 | 文書確認 + CSV 集計 | souWndscape ラベルは `1478 -> 739` 重複、ラベル付きファイル `66` 本、`9` サイト、`train_audio` primary 無し 28 クラスは soundscape には出現 | loader の正規化とリーク防止 CV は前処理ではなく必須要件 | 重複除去、マルチラベル展開、site grouping 実装へ進む |
| 2026-05-01 | kernel 監査 | `doc/kernel/2026` を類型化し、代表 notebook を精読 | notebook 分類 + 代表読解 | 公開 kernel は `Perch+ProtoSSM` 系と `CNN/SED+distill` 系に集中。前者は強いが `0.93` 前後で飽和傾向、後者は pseudo-label と組み合わせて `~0.935` まで伸びる | `Perch` は残すが、SSM 微調整一本足打法にはしない | `Perch` probe と distilled SED baseline の再現を優先 |
| 2026-05-01 | 過去解法監査 | `doc/solution/2023-2025` の summary と上位 raw writeup を横断比較 | 年度横断比較 | 毎年勝っている要素は `SED`、soundscape ドメイン適応、smoothing / rank-aware 後処理、推論最適化。`2025` は `20 秒` チャンク、iterative noisy student、分類群専用枝が強い | 2026 の主戦略は unlabeled soundscape を使う fast CNN/SED self-training に置くべき | `20 秒 SED` 教師学習と pseudo-label cache 設計へ進む |
| 2026-05-02 | ローカル基準線確認 | `notebook.ipynb` を精読し、ユーザー報告 `LB 0.930` の基盤構成を確認 | notebook 構造確認 | 構成は `Perch ONNX + prior + MLP probe + LightProtoSSM + ResidualSSM + 後処理`。OOF は `filename GroupKFold` ベースで、fully-labeled `59` files を中心に回る | モデル/提出基盤としては採用可。ただし CV は site-aware と fold-safe prior へ拡張が必要 | `notebook.ipynb` を基準線にして CV と評価系を強化する |
| 2026-05-02 | Perch 実学習 CV | `uv` 依存を追加し、Perch `SavedModel` をローカル展開。`scripts/experiment/perch_probe_cv.py` で deduped labeled soundscapes に対する実学習 OOF を計測 | Perch 埋め込み抽出 + grouped CV + macro ROC-AUC | raw Perch は `full59_raw_perch_filegkf5=0.729017`, `all66_raw_perch_filegkf5=0.736658`。`embedding PCA + balanced logistic` をそのまま置換すると悪化したが、`min_pos>=3` クラスだけ raw logits に logit-blend (`alpha=0.4`) する `probe_pca_blend` で `full59=0.886197`, `all66_filegkf5=0.891374`, `all66_sitebalanced3=0.882219` | Perch 単体を捨てず、学習器は補正として混ぜる方が安定して強い。partial files を含む `66 files` 全量利用も有効 | この split 定義のまま ProtoSSM / ResidualSSM / prior を fold-safe に足し、`0.891374` を次の更新基準にする |
| 2026-05-02 | CV bundle 保存と Kaggle 配布 | `scripts/experiment/train_perch_probe_models.py` を追加し、3 パターンの full-fit + fold bundle を `output/models/cv_models/` に保存。notebook 互換 cache `perch_meta.parquet` / `perch_arrays.npz` も root に生成し、Kaggle Dataset `suzukitaichi/birdclef-2026-perch-probe-cv-models` へアップロード | `uv run python scripts/experiment/train_perch_probe_models.py` + `quality_gate.py all` + Kaggle CLI upload | `main_all66_sitebalanced3=0.882219`, `secondary_all66_filegkf5=0.891374`, `stress_all66_siteholdout3=0.857185`。Kaggle では root cache はそのまま、`cv_models/` は `cv_models.zip` として載る | notebook 互換 cache と CV bundle 配布は両立できる。主ゲートは site-balanced、副ゲートは file GKF、stress は site holdout が妥当 | notebook 側の artifact loader と fold-safe 後処理統合へ進む |
| 2026-05-02 | full-fit probe submit notebook 化 | `notebook.ipynb` を saved full-fit probe submit 用に修正し、`scripts/experiment/prepare_kaggle_full_fit_kernel.py` で Kaggle kernel bundle を生成。kernel `suzukitaichi/birdclef-2026-full-fit-probe-submit` を push | notebook JSON 検証 + `quality_gate.py all` + `kaggle kernels push` + `kaggle kernels status` | submit 経路は `cv_models.zip` から `full_fit/probe_bundle.joblib` を読み、Perch test cache に対して直接推論する形になった。Kaggle kernel は push 成功、status は `RUNNING` | 今回の full-fit probe 再現に submit 時学習は不要。API 実行で必要なのは Perch test 抽出と bundle 推論だけ | 実行完了後に `submission.csv` と runtime を確認し、必要なら source attach を微調整する |
| 2026-05-02 | zip 内 bundle path 修正 | `notebook.ipynb` の `_load_full_fit_probe_bundle()` を修正し、`cv_models.zip` 内の member 名を suffix 探索で解決するようにした | notebook JSON 検証 + kernel bundle 再生成 + `kaggle kernels push` | `cv_models/{pattern}/...` 完全一致で失敗していたケースに対して、zip 内 path の揺れを吸収できるようになった。Kaggle kernel version 2 を push 済みで status は `RUNNING` | Kaggle の zip 化では archive 内 root が固定ではない前提で loader を書くべき | 実行完了後に bundle 読み込みエラーが解消したかを確認する |
| 2026-05-02 | Kaggle mount path 修正 | ユーザー報告の実パス `/kaggle/input/datasets/suzukitaichi/birdclef-2026-perch-probe-cv-models/cv_models` に合わせて、`notebook.ipynb` の full-fit bundle loader に mount path 候補を追加し、最後は `/kaggle/input` 全体を suffix 探索するようにした | notebook JSON 検証 + kernel bundle 再生成 + `kaggle kernels push` + `kaggle kernels status` | dataset root 想定だけでは足りず、`cv_models` 直下 mount にも対応が必要だった。Kernel version 3 を push 済みで status は `RUNNING` | Kaggle の attach 形は一意ではないので、入力 root からの discovery を最後の fallback に持つ方が安全 | version 3 の実行完了を待ち、bundle 読み込みエラーが消えたか確認する |
| 2026-05-02 | sklearn 互換推論修正 | `notebook.ipynb` の full-fit probe submit 経路で `LogisticRegression.predict_proba()` を使わず、`coef_` / `intercept_` から確率を手計算するようにした | notebook JSON 検証 + kernel bundle 再生成 + `kaggle kernels push` + `kaggle kernels status` | Kaggle 側で `LogisticRegression` に `multi_class` が無く `predict_proba()` が落ちるケースを回避できた。Kernel version 4 を push 済みで status は `RUNNING` | cross-version の sklearn object は method 呼び出しより attribute ベース推論の方が安全 | version 4 の完走と `submission.csv` 生成を確認する |
| 2026-05-02 | full-fit probe submit 実績 | ユーザー確認ベースで Kaggle notebook `birdclef-2026-full-fit-probe-submit` version 5 の提出結果を記録 | Kaggle 実行結果の報告 | version 5 は score `0.891` を記録 | full-fit probe submit 経路は実際に提出スコアまで到達している。以後の notebook 修正はこの `0.891` を最低比較線として扱う | version 6 以降で ONNX-first + tf-wheels 版が `0.891` を維持できるか確認する |
| 2026-05-02 | tf-wheels + ONNX-first 化 | `ashok205/tf-wheels` の output wheel 名を確認し、Kaggle kernel metadata に `kernel_sources` として追加。`notebook.ipynb` は ONNX が見えるとき SavedModel を先に読まず、`labels.csv` も `perch_v2.onnx` 同梱物を優先して解決するよう修正 | `kaggle kernels output ashok205/tf-wheels` で wheel 確認 + notebook JSON 検証 + `quality_gate.py all` + kernel bundle 再生成 + `kaggle kernels push` | `ashok205/tf-wheels` には `tensorflow-2.20.0-*.whl` と `tensorboard-2.20.0-*.whl` があり、notebook の wildcard install と整合した。Kernel version 6 を push 済みで status は `RUNNING` | TF は fallback として残しつつ、submit 実運用は ONNX-first に寄せるのが安全。`archive (1)` と同じ 3 ファイル構成を input でそのまま扱える | version 6 の完走と `submission.csv` / runtime を確認する |
| 2026-05-02 | 金メダル計画策定 — 過去解法横断分析 | `doc/solution/2023-2025` の summary, raw writeup, CSV 比較表を横断分析し、年度共通の勝因を抽出。既存ワークスペースの Gap 分析を実施 | 文書精読 + 年度横断比較 + 現状 src 構成との差分分析 | 金メダル共通勝因: SED ヘッド (全年全上位), iterative pseudo-labeling (2025 最大勝因), soundscape ドメイン適応, 軽量 CNN, 推論最適化 (OpenVINO)。2025-1 位の具体パラメータ (20 秒, n_mels=224, MixUp ratio=1.0, power transform, 4 回反復) を記録。現状実装の Gap は約 70% (SED 学習基盤・self-training・推論最適化が全て未実装) | 未確定事項 4 件を解決 (主力枝=SED, pseudo 比率=1.0, 専用枝=有効, 推論形式=OpenVINO)。金メダルのレシピは 2025 が明確に示しており実装は定型的。最大リスクは GPU 資源と 90 分制約 | `current.md`, `strategy.md`, `todo.md` を 4 フェーズ計画に更新。Phase 1 の実装に進む |
| 2026-05-02 | Phase 0.5 方針決定 — Perch + train_audio 適応学習 | 現ベースラインが soundscape 708 窓のみで学習している弱点を分析し、`train_audio` の Perch frozen embedding で 234 クラス MLP head を学習する方針を策定 | ベースライン構造分析 + Perch マッピング確認 (203/234 種対応、31 種 unmapped) + 既存コード (`perch_probe_cv.py`, `train_perch_probe_models.py`) の再利用可能性確認 | Perch 本体 fine-tune は重すぎる。frozen embedding + MLP head が安全かつ低コスト。`train_audio` で head 学習→Perch logits と blend→soundscapes で calibration の 3 段階構成が最適 | `train_audio` は本番ドメインと違うので強く学習しすぎるとドメインギャップ沼にはまる。blend 比率と calibration が鍵。Perch 本体 fine-tune は ONNX パスを壊すのでこの段階ではやらない | `scripts/experiment/extract_train_audio_embeddings.py` 作成 → `train_audio_mlp_head.py` 作成 → logit blend 検証 → LB 確認 |
| 2026-05-02 | ver8 初回 artifact 化 | `extract_train_audio_embeddings.py`, `train_audio_mlp_head.py`, `prepare_kaggle_ver8_kernel.py`, `run_ver8_train_audio_submit.py` を整備し、まず既存 cache `data/models/train_audio_perch_cache.npz` (`35,549` single-chunk samples) で `train_audio` 適応 MLP head を学習。ver8 submit notebook bundle も生成 | `uv run python scripts/quality_gate.py format`, `lint`, subset smoke (抽出・学習・orchestrator), full head 学習 (`epochs=10`) + kernel bundle 生成 | `output/models/train_audio_mlp_head_bundle.joblib` と `train_audio_mlp_head_meta.json` を生成。head 学習は `31,994 train / 3,555 val` で走り、weighted BCE は `train=0.00490`, `val=0.00888`。`output/kaggle_kernel/ver8_train_audio_mlp_submit/kernel-metadata.json` も生成済み | ver8 の最初の提出候補は「既存 single-chunk cache を使う train_audio 適応 head + 既存 full-fit probe」の構成で成立した。全 5 秒 chunk cache は Phase 0.5 の次改善として残る | Kaggle Dataset version を切って head bundle を upload → ver8 kernel push → submit 完走確認。次に full-chunk cache 版へ置き換える |
| 2026-05-02 | ver8 初回提出結果 | ユーザー確認ベースで Kaggle notebook `birdclef-2026-full-fit-probe-ver8-submit` version 2 の提出結果を記録 | Kaggle 実行結果の報告 | version 2 は score `0.853` を記録 | 既存 single-chunk cache 由来の `train_audio` head を、そのまま full-fit probe 後段に blend するだけでは baseline `0.891` を下回った。`train_audio` 側の利得よりドメインギャップ/校正ずれの悪影響が強い | ver8 は full 5 秒 chunk cache、soundscape calibration、class-wise blend 制御ありで再実験する |
| 2026-05-02 | Phase 0.5 深掘り — low-LR 継続学習パイプライン | `train_audio_mlp_head.py` と `run_ver8_train_audio_submit.py` を読み、ver8 初回失敗後の設計を再整理 | 実装監査 + 戦略再設計 | 現行実装は既に `stage1_train_audio -> stage2_soundscape` の継続学習構造を持つが、default は `stage1_learning_rate=3e-4`, `stage2_learning_rate=1e-4`, `stage2_val_fraction=0.0`。初回 ver8 は single-chunk cache と固定比率 blend が中心で、target-domain 検証が弱かった | Phase 0.5 の主眼は `train_audio` の別枝追加ではなく、低LR warm start 後に `train_soundscapes` で継続学習した同一 head を作ること。採用判断は train_audio loss ではなく soundscape OOF で行うべき | full 5 秒 cache を再生成し、low-LR sweep と soundscape OOF 由来の class-wise `alpha` / `bias` / `temperature` 校正を優先する |
| 2026-05-02 | 継続学習 head honest CV 実装 | `scripts/experiment/train_audio_probe_blend_models.py` を追加し、`probe` fold bundle を固定したまま継続学習 head の OOF と class-wise `alpha` blend を評価できるようにした。submit notebook patch も `probe + head` blend へ更新 | 新規 CV スクリプト追加 + `prepare_kaggle_ver8_kernel.py` 更新 + main gate で honest CV | `main_all66_sitebalanced3` で `stage1=4ep@1e-4 -> stage2=8ep@2e-5` は `probe=0.882219`, `head=0.667842`, `blend=0.869417`。`stage1=0, stage2=8ep@5e-5` でも `head=0.377423`, `blend=0.874181`。どちらも baseline 未満 | current single-recording cache では継続学習 head の補助枝はまだ弱い。OOF で改善が出るまでは submit に混ぜるべきではない | full 5 秒 chunk cache の生成を再開し、その cache で同じ honest CV をやり直す。現時点の Kaggle submit は `alpha=0` safe fallback で probe baseline を維持する |
| 2026-05-03 | SED teacher dataset 実装 | `src/data/dataset.py` を追加し、`train_audio` 20 秒 clip に対して `4 x 5 秒` の Perch teacher logits と weak label を束ねる indexable dataset を実装。`src/core/config.py` に distillation 設定を追加し、unit test で tail padding と旧 cache 拒否も確認できるようにした | `uv run python scripts/quality_gate.py format`, `lint`, `typecheck`, `arch`, `test` | 実装ベースでは、full 5 秒 chunk cache があれば `train_audio` を 20 秒 clip 単位へ変換し、tail clip には `teacher_mask` を立てて蒸留 loss 側で無効化できる。旧 `train_audio_perch_cache.npz` のような single-chunk cache はこの用途に不十分 | 20 秒 SED + Perch teacher の最初の論点は trainer より前に「データ整合」を固めることだった。teacher cache の必須キーを固定したので、次の loss/trainer 実装がぶれにくくなった | `src/training/losses.py` に weak BCE + masked distill loss を追加し、`train_sed.py` の入口を作る |
| 2026-05-03 | SED CV stress split 接続 | `src/data/cv_splits.py` を共通 group-kfold 化し、`site_holdout_folds()` を追加。`scripts/experiment/train_sed.py` の CLI に `site_holdout` を露出し、unit test を追加した | `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/quality_gate.py lint`, `typecheck`, `test` | SED 側の共通 CV 層でも、Perch 系の honest CV と同じ `site_holdout` stress gate を選べるようになった。`tests/test_cv_splits.py` を含めて `lint/typecheck/test` は通過 | Phase 1 の評価入口は `file_gkf` と `site_balanced` だけでは不十分で、stress split も最初から共通化しておく方が比較がぶれない | 次は `train_sed.py` の最小 smoke run を作り、summary artifact と fold 出力の形を固定する |

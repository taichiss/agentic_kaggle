# 実験ログ

| 日付 | テーマ | 変更点 | 検証方法 | 結果 | 学び | 次 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-01 | discussion 監査 | `doc/discussion/2026` を精読し、主要主張をローカル CSV で照合 | 文書確認 + CSV 集計 | soundscape ラベルは `1478 -> 739` 重複、ラベル付きファイル `66` 本、`9` サイト、`train_audio` primary 無し 28 クラスは soundscape には出現 | loader の正規化とリーク防止 CV は前処理ではなく必須要件 | 重複除去、マルチラベル展開、site grouping 実装へ進む |
| 2026-05-01 | kernel 監査 | `doc/kernel/2026` を類型化し、代表 notebook を精読 | notebook 分類 + 代表読解 | 公開 kernel は `Perch+ProtoSSM` 系と `CNN/SED+distill` 系に集中。前者は強いが `0.93` 前後で飽和傾向、後者は pseudo-label と組み合わせて `~0.935` まで伸びる | `Perch` は残すが、SSM 微調整一本足打法にはしない | `Perch` probe と distilled SED baseline の再現を優先 |
| 2026-05-01 | 過去解法監査 | `doc/solution/2023-2025` の summary と上位 raw writeup を横断比較 | 年度横断比較 | 毎年勝っている要素は `SED`、soundscape ドメイン適応、smoothing / rank-aware 後処理、推論最適化。`2025` は `20 秒` チャンク、iterative noisy student、分類群専用枝が強い | 2026 の主戦略は unlabeled soundscape を使う fast CNN/SED self-training に置くべき | `20 秒 SED` 教師学習と pseudo-label cache 設計へ進む |
| 2026-05-02 | ローカル基準線確認 | `notebook.ipynb` を精読し、ユーザー報告 `LB 0.930` の基盤構成を確認 | notebook 構造確認 | 構成は `Perch ONNX + prior + MLP probe + LightProtoSSM + ResidualSSM + 後処理`。OOF は `filename GroupKFold` ベースで、fully-labeled `59` files を中心に回る | モデル/提出基盤としては採用可。ただし CV は site-aware と fold-safe prior へ拡張が必要 | `notebook.ipynb` を基準線にして CV と評価系を強化する |
| 2026-05-02 | CV proxy 検証 | `scripts/experiment/cv_soundscape_validation.py` を追加し、deduped soundscape labels に対して fold-safe site/hour prior の proxy OOF を比較 | 純標準ライブラリで split + macro ROC-AUC を再計算 | `notebook_full59_filegkf5_sitehour` は `0.495997`、`all66_filegkf5_sitehour` は `0.489057`、`all66_sitebalanced_file3_sitehour` は `0.531146`。site-balanced 3-fold は fold active classes も `42-53` と安定。pure site holdout 3-fold は `0.193321` で厳しすぎる | notebook の `59 full files` 固定より、`66 files` 全量 + site-balanced file split の方が local gate として扱いやすい。主ゲートは二段化するのが自然 | 実モデルの OOF 経路をこの split に移植し、proxy ではなく本番 pipeline で再比較する |
| 2026-05-02 | Perch 実学習 CV | `uv` 依存を追加し、Perch `SavedModel` をローカル展開。`scripts/experiment/perch_probe_cv.py` で deduped labeled soundscapes に対する実学習 OOF を計測 | Perch 埋め込み抽出 + grouped CV + macro ROC-AUC | raw Perch は `full59_raw_perch_filegkf5=0.729017`, `all66_raw_perch_filegkf5=0.736658`。`embedding PCA + balanced logistic` をそのまま置換すると悪化したが、`min_pos>=3` クラスだけ raw logits に logit-blend (`alpha=0.4`) する `probe_pca_blend` で `full59=0.886197`, `all66_filegkf5=0.891374`, `all66_sitebalanced3=0.882219` | Perch 単体を捨てず、学習器は補正として混ぜる方が安定して強い。partial files を含む `66 files` 全量利用も有効 | この split 定義のまま ProtoSSM / ResidualSSM / prior を fold-safe に足し、`0.891374` を次の更新基準にする |
| 2026-05-02 | CV bundle 保存と Kaggle 配布 | `scripts/experiment/train_perch_probe_models.py` を追加し、`site-balanced 3-fold` / `file GroupKFold 5-fold` / `site holdout 3-fold` の full-fit + fold bundle を `output/models/cv_models/` に保存。notebook 互換 cache `perch_meta.parquet` / `perch_arrays.npz` も root に生成し、Kaggle Dataset `suzukitaichi/birdclef-2026-perch-probe-cv-models` へアップロード | `uv run python scripts/experiment/train_perch_probe_models.py` + `quality_gate.py all` + Kaggle CLI upload | `main_all66_sitebalanced3=0.882219`, `secondary_all66_filegkf5=0.891374`, `stress_all66_siteholdout3=0.857185`。Kaggle では root cache はそのまま、`cv_models/` は `cv_models.zip` として載る | notebook 互換 cache と CV bundle 配布は両立できる。最高スコアは file GKF だが、主ゲートは site-balanced の方が honest。site holdout は破綻しないが fold 偏りが強い | notebook 側から Kaggle artifact を直接読む loader と、prior / ResidualSSM / threshold の fold-safe 再構成を進める |
| 2026-05-02 | full-fit probe submit notebook 化 | `notebook.ipynb` を saved full-fit probe submit 用に修正し、`scripts/experiment/prepare_kaggle_full_fit_kernel.py` で Kaggle kernel bundle を生成。kernel `suzukitaichi/birdclef-2026-full-fit-probe-submit` を push | notebook JSON 検証 + `quality_gate.py all` + `kaggle kernels push` + `kaggle kernels status` | submit 経路は `cv_models.zip` から `full_fit/probe_bundle.joblib` を読み、Perch test cache に対して直接推論する形になった。Kaggle kernel は push 成功、status は `RUNNING` | 今回の full-fit probe 再現に submit 時学習は不要。API 実行で必要なのは Perch test 抽出と bundle 推論だけ | 実行完了後に `submission.csv` と runtime を確認し、必要なら source attach を微調整する |
| 2026-05-02 | zip 内 bundle path 修正 | `notebook.ipynb` の `_load_full_fit_probe_bundle()` を修正し、`cv_models.zip` 内の member 名を suffix 探索で解決するようにした | notebook JSON 検証 + `prepare_kaggle_full_fit_kernel.py` 再生成 + `kaggle kernels push` | `cv_models/{pattern}/...` 完全一致で失敗していたケースに対して、zip 内 path の揺れを吸収できるようになった。Kaggle kernel version 2 を push 済みで status は `RUNNING` | Kaggle の zip 化では archive 内 root が固定ではない前提で loader を書くべき | 実行完了後に bundle 読み込みエラーが解消したかを確認する |
| 2026-05-02 | Kaggle mount path 修正 | ユーザー報告の実パス `/kaggle/input/datasets/suzukitaichi/birdclef-2026-perch-probe-cv-models/cv_models` に合わせて、`notebook.ipynb` の full-fit bundle loader に mount path 候補を追加し、最後は `/kaggle/input` 全体を suffix 探索するようにした | notebook JSON 検証 + `prepare_kaggle_full_fit_kernel.py` 再生成 + `kaggle kernels push` + `kaggle kernels status` | dataset root 想定だけでは足りず、`cv_models` 直下 mount にも対応が必要だった。Kernel version 3 を push 済みで status は `RUNNING` | Kaggle の attach 形は一意ではないので、入力 root からの discovery を最後の fallback に持つ方が安全 | version 3 の実行完了を待ち、bundle 読み込みエラーが消えたか確認する |
| 2026-05-02 | sklearn 互換推論修正 | `notebook.ipynb` の full-fit probe submit 経路で `LogisticRegression.predict_proba()` を使わず、`coef_` / `intercept_` から確率を手計算するようにした | notebook JSON 検証 + kernel bundle 再生成 + `kaggle kernels push` + `kaggle kernels status` | Kaggle 側で `LogisticRegression` に `multi_class` が無く `predict_proba()` が落ちるケースを回避できた。Kernel version 4 を push 済みで status は `RUNNING` | cross-version の sklearn object は method 呼び出しより attribute ベース推論の方が安全 | version 4 の完走と `submission.csv` 生成を確認する |
| 2026-05-02 | full-fit probe submit 実績 | ユーザー確認ベースで Kaggle notebook `birdclef-2026-full-fit-probe-submit` version 5 の提出結果を記録 | Kaggle 実行結果の報告 | version 5 は score `0.891` を記録 | full-fit probe submit 経路は実際に提出スコアまで到達している。以後の notebook 修正はこの `0.891` を最低比較線として扱う | version 6 以降で ONNX-first + tf-wheels 版が `0.891` を維持できるか確認する |
| 2026-05-02 | tf-wheels + ONNX-first 化 | `ashok205/tf-wheels` の output wheel 名を確認し、Kaggle kernel metadata に `kernel_sources` として追加。`notebook.ipynb` は ONNX が見えるとき SavedModel を先に読まず、`labels.csv` も `perch_v2.onnx` 同梱物を優先して解決するよう修正 | `kaggle kernels output ashok205/tf-wheels` で wheel 確認 + notebook JSON 検証 + `quality_gate.py all` + kernel bundle 再生成 + `kaggle kernels push` | `ashok205/tf-wheels` には `tensorflow-2.20.0-*.whl` と `tensorboard-2.20.0-*.whl` があり、notebook の wildcard install と整合した。Kernel version 6 を push 済みで status は `RUNNING` | TF は fallback として残しつつ、submit 実運用は ONNX-first に寄せるのが安全。`archive (1)` と同じ 3 ファイル構成を input でそのまま扱える | version 6 の完走と `submission.csv` / runtime を確認する |
| 2026-05-02 | 音声 EDA ツール実装 | `scripts/audio_eda.py` と `src/` 配下にローカル HTTP ベースの音声ブラウザを追加 | `quality_gate.py all` + ローカル起動スモークテスト | `train_audio` `35,549` 行、`train_soundscapes` ディレクトリ全体 `10,658` files、重複除去済みラベルは `66` files / `739` windows。これらを横断して、再生・波形・スペクトログラム・soundscape 窓ラベルをブラウザで見られるようになった | BirdCLEF は数値ログだけだとラベルノイズや窓位置の違和感を拾いにくい。軽い定性 EDA 導線を常設しておく価値が高い | model score / pseudo-label overlay を足して、誤検知パターン確認まで一気通貫にする |
| 2026-05-02 | EDA 拡張 | `audio_eda` に前後コンテキスト再生、species 集計、周波数傾向分析、アノテーション保存を追加 | `quality_gate.py all` + localhost API / UI スモークテスト | soundscape 窓の前後コンテキスト再生、周波数プロファイルと `centroid / rolloff / flatness` の確認、species ごとの `train_audio` 件数・soundscape 出現数・rating 分布、JSONL 保存の誤検知メモが同じ画面で使えるようになった | 定性監査は単一ビューに寄せた方が、ラベルノイズ、帯域偏り、rare class の弱さを一度に判断しやすい | pseudo-label / OOF score overlay と、保存メモの再集計導線を次に足す |
| 2026-05-02 | species 分布図 export | `scripts/export_species_distribution.py` と SVG renderer を追加し、`doc/overview/2026/species_distribution.svg` を生成 | `uv run python -m unittest tests.test_audio_catalog` + export 実行 + `quality_gate.py all` | `train_audio 206 species / 35,549 recordings`、labeled soundscape `75 species / 739 windows / 66 files` を 3 面 ranking 図として保存できた | UI 内の集計だけでなく、静的な図として残すと long-tail と soundscape 側偏りを sampling / weighting の議論に持ち込みやすい | rare class と soundscape-only class の扱いを整理し、学習時の class weighting 仮説に落とす |
